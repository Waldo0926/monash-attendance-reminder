# 更新日志

## v1.3.46
v1.3.45 那次修复是真的修对了，但没修完。它让负责拼接行文字的代码不再被那种只是长得像签到码的乱码片段骗过去，但一次真实的重新导出显示，同一行（FIT2102 Workshop 01，8 月 18 日）还是空白。真正的原因是更早的一个判断，它把那串乱码里碰巧凑出来的假签到码当成已经够用了，导致专门给签到码那一列做的、带白名单限制的裁剪识别根本没有跑起来，v1.3.45 修好的部分从没机会派上用场。这个版本把那个更早的判断拿掉了，改成只要图片形状像是有一列签到码就去跑裁剪识别，跟 v1.3.44 里用过的原则一样。这次用真实的乱码文字和真实的裁剪结果，一起跑了 shared.js 里实际在用的匹配函数做了端到端验证。有一点如实说明：这一行背后的原图只有 841x42px，裁剪识别在这个分辨率下还是会把一个字符认错（T 认成 I），建议对照真实的 Ed 帖子肉眼核对一下。

## v1.3.45
把拼接代码改成了，只有当一行自己的识别结果真的跟裁剪识别出来的签到码一致时，才允许拿它去挡住裁剪结果，而不是任何长得像签到码的乱码片段都能挡住。这个改动本身是对的，只是 v1.3.46 才发现它对本来要修的那一行根本没机会派上用场。

## v1.3.44
在表格分支（不是独立小图那种）里拿掉了一个类似的、基于数量对比的补救触发条件。那个条件在整行一起丢失、签到码数量和行数依然同步对得上的时候，会让丢失的整行永远得不到补救。

## v1.3.43
一次修了三个各自独立、导致 FIT2102、FIT2109、FIT3162 签到码彻底找不到的问题：Ed 讨论帖列表的懒加载、课程链接被同时打上所有已选课程代码而不是它自己的、以及 Moodle 的 My units 页面需要比旧等待时间长得多才能渲染出来。

## v1.3.42
加的调试日志让上面这三个问题第一次变得看得见，而不是继续瞎猜。

## v1.3.41
修的是 Ed 备用路径里固定只挑 2 个帖子的数量上限问题，这是跟上面懒加载相关、但不同的另一个问题。

## v1.3.40
修复了打开的 Gmail 邮件里，签到码表格是贴上去的截图时不会被送去做文字识别的问题。

## v1.3.39
补全了 Attendance 网站自己已经看不到的更早几周的课程记录。

## v1.3.38
在导出结果旁边加了已请求范围和实际返回范围的对比提示，并修复了调试日志的一个写入竞争问题。

## v1.3.37
修复了给单周扫描设计的固定 Gmail 邮件数量上限导致的签到码丢失问题。
