# Monash Attendance Helper

[![类型](https://img.shields.io/badge/%E7%B1%BB%E5%9E%8B-Chrome_%E6%89%A9%E5%B1%95-2563eb?style=for-the-badge)](#)
[![技术](https://img.shields.io/badge/%E6%8A%80%E6%9C%AF-JavaScript-7c3aed?style=for-the-badge)](#)
[![平台](https://img.shields.io/badge/%E5%B9%B3%E5%8F%B0-Windows_%7C_macOS_%7C_Linux-16a34a?style=for-the-badge)](#平台支持)
[![许可证](https://img.shields.io/badge/%E8%AE%B8%E5%8F%AF%E8%AF%81-%E4%BF%9D%E7%95%99%E6%89%80%E6%9C%89%E6%9D%83%E5%88%A9-dc2626?style=for-the-badge)](#)

[English](README.md) | **简体中文**

> 当前扩展版本：**v1.3.46** —— v1.3.45 那次修复是真的修对了一个 bug，但没修完：它让负责拼接的代码不再被那种"长得像签到码"的乱码片段骗过去，可一次真实的重新导出显示，同一行（FIT2102 Workshop 01，8 月 18 日）还是空白。重新对照真实图片和真实代码才找到原因：那串乱码（还是 v1.3.45 里说的"CcQlTe"后面跟一个重音符号，转大写后碰巧凑出 5 个字符）同时还满足了另一个更早的判断——"整行文字里已经识别出来的签到码数量，是不是已经跟行数对上了"——这个判断决定要不要去跑专门给签到码那一列做的、带白名单限制的裁剪识别。因为这个假签到码让数量看起来已经"够了"，裁剪识别压根就没被跑起来，v1.3.45 修好的那部分代码根本没有机会派上用场。这个版本把这个更早的数量判断也拿掉了，改成只要图片形状看起来像是有一列签到码，就去跑裁剪识别——这跟 v1.3.44 里已经用过的"相信图片形状，而不是相信一个可能被假阳性骗过的数字"是同一个原则。这次是真正端到端验证过的，不只是看代码：把真实的乱码文字和真实的裁剪识别结果，一起丢进 shared.js 里那个真正在用的、导出的匹配函数里跑了一遍，确认这一行现在真的能得到一个候选值，而不是空白。v1.3.45 里说的那个提醒还是有效：这一行背后的原图确实非常小，带白名单限制的裁剪识别在这个分辨率下还是把一个字符认错了（T 认成 I），所以这一行依然值得肉眼核对一下真实的 Ed 帖子，不能保证百分之百准确。（v1.3.45 把拼接代码改成了只有当这一行自己的识别结果，和列裁剪识别结果真的一致时，才允许压过裁剪结果——这个改动本身是对的，只是这次才发现它一直没机会被真正用上；v1.3.44 在整行代码的另一处（非独立小图的表格分支）拿掉了一个类似的、基于数量对比的补救触发条件，那个条件在整行一起消失、签到码数量和行数依然同步对得上的时候，会让丢失的整行永远得不到补救；v1.3.43 一次修了三个各自独立、导致 FIT2102/FIT2109/FIT3162 签到码彻底找不到的问题：Ed 讨论帖列表的懒加载、课程链接被同时打上所有已选课程代码而不是它自己的、以及 Moodle "My units" 页面需要比旧等待时间长得多才能渲染出来；v1.3.42 加的调试日志让上面这三个问题第一次变得看得见，而不是继续瞎猜；v1.3.41 修的是 Ed 备用路径里固定只挑 2 个帖子的数量上限问题，这是和上面"加载更多"那个问题相关但不同的另一个问题；v1.3.40 修复的是打开的 Gmail 邮件里，签到码表格是贴上去的截图时不会被送去做文字识别的问题；v1.3.39 补全了 Attendance 网站自己已经看不到的更早几周的课程记录；v1.3.38 在导出结果旁边加了"已请求范围 vs 实际返回范围"的对比提示，并修复了调试日志的一个写入竞争问题；v1.3.37 修复的是给单周扫描设计的固定 Gmail 邮件数量上限导致的签到码丢失问题。）

一个注重隐私、**跨平台**的 Chrome 扩展，帮助 Monash 学生自动发现近期需要处理的考勤活动，从 Gmail、Ed Discussion 和 Moodle 中寻找对应的 Attendance Code，在提交前集中核对，并且只会在学生明确确认后才提交。

默认模式**不需要手动填写完整课表**。扩展会读取当前 Chrome Profile 已登录的 Monash Attendance 中的近期课程活动，同时保留已经完成签到的记录，再从当前已登录的 Monash 学习平台中寻找对应签到码。

> **重要：** 本项目不是无人值守的自动签到机器人。它只负责帮助整理和核对考勤信息；真正提交前，学生必须确认自己确实参加了对应课程。

## 功能

- 自动从 **Monash Attendance** 发现近期课程活动，扫描范围往前覆盖过去一周、往后延伸到本周日，兼容会提前几天显示课程表的账号。
- 已经在 Attendance 中显示完成的课程仍会保留，并标记为已完成，而不是直接隐藏。
- 同一天存在多节已完成课程时，会按课程、活动、班号和时间分别保存，不会互相覆盖。
- 按以下顺序寻找签到码：
  1. **Gmail**：优先检查近期 Attendance 相关邮件；
  2. **Ed Discussion**：如果课程使用 Ed，则检查近期 Attendance / Attendance Code 帖子；
  3. **Moodle**：继续进入对应课程、近期 Week / section 页面以及 Attendance activity 查找。
- 对文字形式的 Attendance 表格直接解析。
- 对以图片形式发布的签到表，可使用扩展内置的本地 OCR。
- 根据可获得的课程代码、活动类型、班号、日期和时间综合匹配签到码。
- 对不确定结果明确标记为需要人工核对，而不是直接猜测。
- 状态按“本周 / 上周”分开显示：本周没找到码大多是老师还没发布，属于正常等待；上周还是没找到，才是需要手动核实的情况。
- 课程还没到上课时间时会显示“未开始”——即使已经提前找到了签到码，也不会显示成可以立即提交，避免误导。
- “扫描”和“最终核对”合并成后台一次性完成，不需要 popup 或确认页全程保持打开就能跑完。
- 关键步骤的日志会持久化存储，并直接显示在确认页面底部的“调试日志”区块，排查问题不需要打开开发者工具。
- 支持自定义 Chrome 提醒时间。
- 提交前必须由学生主动勾选出席声明。
- 已完成课程或还没开始的课程都不会进入提交路径。
- 保留手动课表模式，作为自动发现失败时的备用方案。

提醒时间使用**运行 Chrome 的电脑当前系统时区**。

## 平台支持

这个项目是 **Chrome 扩展 + 浏览器端 Web 配置器**，并不是 macOS 原生应用。只要桌面 Chrome/Chromium 环境支持项目所需的扩展 API，同一套安装和使用方式可用于 **Windows、macOS 和 Linux**。

扩展本身没有 macOS 专属运行时依赖。仓库中少量与 macOS 开发环境有关的注释或测试，仅用于开发/回归场景，不代表用户端只能在 macOS 上运行。

## 自动发现流程

建议让以下服务保持登录在**同一个 Chrome Profile** 中：

- Monash Attendance
- Gmail
- Monash Moodle
- Ed Discussion（如果你的课程使用 Ed）

扩展的大致查找流程如下：

```text
Monash Attendance
  ↓
发现近期课程 + 判断是否已经完成签到
  ↓
Gmail
  ↓ 未解决
Ed Discussion
  ↓ 未解决
Moodle
  ↓
根据课程 / 活动 / 班号 / 日期 / 时间进行匹配
  ↓
最终重新核对 Attendance 完成状态
  ↓
确认页面
  ↓
学生本人只确认并提交未完成记录
```

已经完成的课程会使用稳定的班次身份保存，因此同一天有多条已签到记录时不会被去重成一条。最终核对会直接读取 Attendance 页面的实时 DOM 来判断每一行是否已完成，不依赖任何注入到页面里的脚本；真正的 `Entry.aspx` 链接仍然代表待填写的 Attendance 记录。“扫描”和“最终核对”这两步现在由后台一次消息处理完成，不再需要 popup 或确认页全程保持打开——之前如果用户在扫描还没跑完时就关掉了 popup，最终核对会被悄悄跳过。

### Moodle 回退查找

Moodle 查找不只是停留在 `My units` 页面。

对于仍未找到签到码的课程，扩展可以继续进入对应课程，检查近期 Week / section 页面，并进一步查找 Attendance 相关 activity。

这对于那些只在 Moodle 里面发布 Attendance Code、而不通过 Gmail 或 Ed 发布的课程尤其有用。

## 已完成签到的课程

确认页不应该只显示“还没填 Attendance”的课程。

已经完成的课程仍然会显示在列表中，并标记为已完成，而且不能再次提交。每张卡片的状态还会加上“本周 / 上周”前缀，这样可以清楚区分：

- **已签到**：Attendance 已显示完成，不能再次提交；
- **未开始**：这节课还没到上课时间——即使已经提前找到签到码，也只作为参考信息展示，勾选框不会开放；
- **高可信**：找到了大概率正确的签到码，等待人工核对后提交；
- **请核对**：找到候选签到码，但需要学生自己确认；
- **未找到**：本周没找到大多是正常等待（老师可能还没发布）；上周还是没找到，代表 Attendance 也没显示完成，建议手动打开来源确认是否真的漏签。

## Chrome 安装方法（Windows / macOS / Linux）

1. 打开这个仓库的 GitHub 页面，点击绿色的 **Code** 按钮，再点 **Download ZIP**。如果你已经会用 git，也可以直接 `git clone`。
2. 找到下载下来的压缩包，一般在“下载”文件夹里，把它解压出来。Windows 上右键点击压缩包选择“全部解压”，macOS 上直接双击压缩包就会自动解压。
3. 打开 Chrome，访问 `chrome://extensions`。
4. 打开右上角的 **开发者模式** 开关。
5. 点击 **加载已解压的扩展程序**。
6. 在弹出的文件选择窗口里，进入刚才解压出来的文件夹，选中里面的 **extension** 子文件夹，不是仓库最外层那个文件夹。
7. 这时候扩展图标应该已经出现在工具栏了。点一下地址栏旁边的拼图图标，把 **Monash Attendance Helper** 固定出来，方便以后点击。
8. 点击扩展图标，或者右键点击它选择“选项”，打开扩展的设置页面。
9. 让 Gmail、Monash Moodle、Ed Discussion（如果你的课程用到）和 Monash Attendance 都登录在扩展所在的同一个 Chrome Profile 里。
10. 在设置页面里，保持自动 Attendance 发现开启，填好本学期 Week 1 星期一的日期，再选好提醒时间。
11. 点击 **保存后立即测试**。这一步会在后台一次性完成普通扫描和最终 Attendance 核对，两步都跑完之后才会显示找到了多少活动和签到码，中途不用一直停留在设置页面。

之后如果你从仓库拉取了更新，记得回到 `chrome://extensions` 对这个未打包的扩展点一下重新加载。页面上显示的版本号应该和 `extension/manifest.json` 里的一致。

## 日常怎么用

装好之后，扩展会一直在后台自动发现和扫描，并在你设置的提醒时间弹出 Chrome 通知。

1. 收到提醒通知时，点一下通知，或者直接点扩展图标，会打开确认页面。
2. 每节课都会显示成一张卡片，标注成已签到、未开始、高可信、请核对或未找到，跟前面“已完成签到的课程”那一节说的是同一套状态。
3. 对于高可信或者请核对的卡片，如果不确定可以对照来源（Gmail、Ed 或 Moodle）核实一下签到码，勾选出席声明后再提交。
4. 已签到和未开始的课程只是展示信息，这里不能提交。
5. 设置页面里的 **导出本学期历史记录** 按钮随时可以用，比如想要留一份完整的学期记录，或者要找任课老师申请人工补签的时候。

请只在自己真正参加了这节课的情况下提交签到。

## 导出本学期历史记录

Attendance 通常只能补填到大约一周前，更早忘记填的课没法再从那边补交。设置页最下面的“导出本学期历史记录”会重新跑一遍从 Week 1 到今天的完整扫描（复用每周扫描同一套发现/匹配逻辑，并优先用已经缓存的签到码，减少重复搜索），把结果导出成一份 CSV 表格，方便留底或者发给任课老师申请人工补签。需要先在上面填好 Week 1 的星期一日期；学期跨度较长时可能要跑几分钟。

## 手动课表模式（备用）

自动发现是推荐模式，但项目仍然保留手动课表作为备用方案。

手动课程配置可以包括：

- 课程代码或课程名；
- Moodle 或 Ed 的来源 URL；
- 可选的 Ed category；
- 你实际参加的 Tutorial、Workshop、Laboratory、Studio、Applied、Seminar 或其他活动；
- 每个活动对应的星期和开始时间。

仓库**不会提供一份要求其他学生直接使用的个人课表**。`examples/` 目录中的配置只用于展示格式，不能假设适用于其他学生。

## Web 配置器

`site/` 目录包含一个图形化配置页面，可以生成：

```text
attendance-helper-config.json
```

生成的文件可以从扩展设置页导入。

## 隐私与安全

扩展不会把以下内容上传到本项目仓库或网站：

- Monash 或 Gmail 密码；
- 登录 Cookie；
- 邮件正文；
- Moodle 或 Ed 页面内容；
- Attendance Code；
- 个人课表数据。

OCR 在扩展本地运行。OCR worker、WebAssembly core 和英文识别模型都随扩展一起打包，不需要把签到表截图发送给第三方 OCR 服务。

## 为什么保留人工确认

Attendance Code 本身不能证明学生真实参加了课程，因此项目刻意保留人工确认步骤：

- 高可信结果可以直接准备到确认页面；
- 不确定结果会明确标记；
- 已完成课程不会重复提交；
- 没有码或匹配不明确时不会随意猜测；
- 提交前必须由学生主动确认出席声明。

## 主要限制

- 每个 Chrome Profile 都需要单独配置。
- Gmail、Moodle、Ed 和 Monash Attendance 的页面结构以后都可能变化。
- 如果登录状态已经过期，仍然需要学生重新登录。
- OCR 可能识别错误，因此被标记为需要核对的图片结果应人工确认。
- 不同课程可能使用完全不同的签到码发布方式。
- 某些 Monash 页面即使内容已经显示，Chrome 仍可能长期显示 `loading`；最终核对会直接读取已经可用的 DOM，但页面本身确实不可用时仍会失败，此时会记录到调试日志里。
- 扫描范围只延伸到本周日，不会提前读取下一教学周的内容。
- 本项目无法保证永久兼容所有课程和未来所有 Monash 页面布局。

## 开发与测试

运行扩展解析与回归测试：

```sh
node --test tests/*.test.mjs
```

运行 Web 配置器测试和 lint：

```sh
cd site
npm ci
npm test
npm run lint
```

当前回归覆盖包括 Gmail 多账号发现、严格班次匹配、OCR 打包、Moodle 回退、已签到记录保留、同日多条已签到记录、completed 与真实 `Entry.aspx` 的区分、最终核对流程、防止重复提交、`executeScript` 超时边界、本周/上周状态标签、未开始课程的占位与提交拦截，以及扫描窗口向本周日延伸。

Pull Request 也会通过 GitHub Actions 自动运行相关检查。

## 项目结构

```text
extension/   Chrome 扩展主体
site/        Web 配置器
examples/    示例配置
scripts/     辅助脚本
tests/       自动化与回归测试
```

## 免责声明

这是一个学生个人开发的辅助工具，**不是 Monash University 官方产品**，也不由 Monash University、学院或课程教学团队运营。

请遵守 Monash 的出勤、学术诚信和 IT 使用规定。只有在你真实参加相应课程时才应提交签到记录。

## License

欢迎身边的 Monash 同学直接使用（比如把这个仓库分享给同学、帮同学装到他们自己的 Chrome 上），但不开放二次修改后重新分发，也不允许 fork 之后删掉人工确认这一步、改成自动提交工具。

如果你想做类似的东西，建议自己重新实现，并且保留真正需要学生确认才能提交的步骤——这是这个项目想强调的学术诚信原则，也是唯一不希望被改动的地方。

使用这个扩展的风险由使用者自行承担，请遵守 Monash 的出勤、学术诚信与 IT 相关政策。